class DashboardDataModel {
  int vendorCount;
  int buyerCount;
  int orderCount;
  int productCount;
  int bannerCount;
  int categoryCount;

  DashboardDataModel({
    this.vendorCount = 0,
    this.buyerCount = 0,
    this.orderCount = 0,
    this.productCount = 0,
    this.bannerCount = 0,
    this.categoryCount = 0,
  });
}
